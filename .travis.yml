language: cpp
before_install:
  - ARD_VER="1.8.5"
  - "/sbin/start-stop-daemon --start --quiet --pidfile /tmp/custom_xvfb_1.pid --make-pidfile --background --exec /usr/bin/Xvfb -- :1 -ac -screen 0 1280x1024x16"
  - sleep 5
  - export DISPLAY=:1.0
  - echo "Downloading version $ARD_VER of the Arduino IDE..."
  - wget http://downloads.arduino.cc/arduino-$ARD_VER-linux64.tar.xz
  - echo "Extracting the Arduino IDE..."
  - tar xf arduino-$ARD_VER-linux64.tar.xz
  - echo "Moving Arduino IDE..."
  - sudo mv arduino-$ARD_VER /usr/local/share/arduino
  - echo "Linking Arduino IDE..."
  - sudo ln -s /usr/local/share/arduino/arduino /usr/local/bin/arduino
  - echo "Removing Arduino IDE ver $ARD_VER tar that was downloaded..."
  - rm arduino-$ARD_VER-linux64.tar.xz
install:
  - arduino --pref "boardsmanager.additional.urls=http://arduino.esp8266.com/stable/package_esp8266com_index.json" --save-prefs
  - arduino --install-boards esp8266:esp8266:2.4.0
  - arduino --board esp8266:esp8266:generic --save-prefs
  - arduino --install-library "Adafruit Unified Sensor"
  - arduino --install-library "DHT sensor library"
  - arduino --install-library "ESP8266 and ESP32 Oled Driver for SSD1306 display"
  - arduino --install-library "MQTT"
  - arduino --pref "compiler.warning_level=all" --save-prefs
script:
  - chmod a+x secrets.sh
  - ./secrets.sh
  - "echo $PWD"
  - "echo $HOME"
  - "ls -al $PWD"
  - arduino -v --verify --board esp8266:esp8266:generic $PWD/ESP8266.cpp
notifications:
  email:
    on_success: change
    on_failure: change